import React from "react";

function Header(props) {
      return (
        <h1>
          {/* uncomment the line below to pass the test */}
          {"hello from the Header!a"}
        </h1>
      )
}

export default Header;
